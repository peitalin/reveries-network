apiVersion: 1

deleteDatasources:
- name: 'Prometheus'

datasources:
- access: 'proxy'
  editable: true
  is_default: true
  name: 'Prometheus'
  type: 'prometheus'
  url: 'http://prometheus:9090'
  version: 1